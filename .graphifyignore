# .graphifyignore — sintaxis idéntica a .gitignore
node_modules/
.next/
dist/
build/
.venv/
vendor/
*.generated.ts
docs/translations/
public/assets/

# --- Necesarias en este repo (no quitar) ---
# Salida del propio graphify: indexarla haria que el grafo se describa a si mismo.
# Excepcion: converted/ guarda los sidecars .md de xlsx/docx, que SI son corpus.
graphify-out/*
!graphify-out/converted/
# Andamiaje que genera `graphify claude/codex install`, no es contenido del proyecto
CLAUDE.md
AGENTS.md
.claude/
.codex/
.cursor/
.github/
.mcp.json

# Export a Google Sheets de la hoja "CRM Prospectos" de crm_jaabogados.xlsx,
# que YA esta en el grafo via graphify-out/converted/. Verificado: los 17
# prospectos y sus datos de contacto (decisor, email, LinkedIn) estan
# duplicados al 100%. Indexarlo creaba nodos gemelos con IDs distintos y
# repartia la centralidad de la matriz entre ellos. Quitar esta linea solo
# si el CSV deja de derivarse del xlsx.
crm_jaabogados.csv
